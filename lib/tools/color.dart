import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF128C7E); //#128c7e;
  static const Color secondaryColor = Colors.grey; // Change this to your secondary color
  static const Color textColor1 = Colors.black; // Change this to your text color
  static const Color textColor2 = Colors.white; // Change this to your text color
  static const Color backgroundColor = Colors.white; // Change this to your background color
  static const Color accentColor = Colors.orange;
  // static Color gradient = Gradient.linear(Color(0xff), to, colors)
}